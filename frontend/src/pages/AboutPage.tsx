export default function AboutPage() {
  return (
    <section>
      <h1>Acerca del proyecto</h1>
      <p>
        Este es el esqueleto de un proyecto colaborativo entre 3 desarrolladores.
        Stack: Vite + React + React Router (frontend), Fastify + TypeScript (backend),
        Postgres local / Supabase en staging y producción.
      </p>
    </section>
  );
}
